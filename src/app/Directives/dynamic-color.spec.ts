import { DynamicColor } from './dynamic-color';

describe('DynamicColor', () => {
  it('should create an instance', () => {
    const directive = new DynamicColor();
    expect(directive).toBeTruthy();
  });
});
